{
  "name": "ipfs-toolbox",
  "packageManager": "yarn@4.0.1",
  "workspaces": [
    "webui",
    "ipfs-cluster-api",
    "tests",
    "testing"
  ],
  "scripts": {
    "watch": "yarn workspaces foreach -A run start",
    "build": "yarn workspaces foreach -A run build",
    "test": "yarn workspace tests run test"
  }
}
